{% style %}
  #shopify-section-{{ section.id }} {
    background: {{ section.settings.color_fondo }};
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
  #shopify-section-{{ section.id }} .tw-pas-texto-eyebrow {
    color: {{ section.settings.color_acento }};
    background: {{ section.settings.color_acento_fondo }};
  }
  #shopify-section-{{ section.id }} .tw-pas-texto-headline {
    color: {{ section.settings.color_titular }};
  }
{% endstyle %}

<section class="tw-section tw-pas-texto">
  <div class="tw-container tw-pas-texto-inner tw-reveal">
    {%- if section.settings.eyebrow != blank -%}
      <span class="tw-eyebrow tw-pas-texto-eyebrow">{{ section.settings.eyebrow }}</span>
    {%- endif -%}
    {%- if section.settings.titular != blank -%}<h2 class="tw-h2 tw-pas-texto-headline">{{ section.settings.titular }}</h2>{%- endif -%}
    {%- if section.settings.texto != blank -%}<p class="tw-body tw-pas-texto-texto">{{ section.settings.texto }}</p>{%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Trotagua – PAS texto",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Texto pequeño superior" },
    { "type": "text", "id": "titular", "label": "Título", "default": "Titular de la etapa" },
    { "type": "textarea", "id": "texto", "label": "Texto", "default": "Texto de la etapa." },
    { "type": "header", "content": "Color" },
    { "type": "color", "id": "color_fondo", "label": "Fondo de la sección", "default": "#FFFFFF" },
    { "type": "color", "id": "color_titular", "label": "Color del título", "default": "#242A1F" },
    { "type": "color", "id": "color_acento", "label": "Color de acento (etiqueta)", "default": "#1F7A72" },
    { "type": "color", "id": "color_acento_fondo", "label": "Color de acento (fondo etiqueta)", "default": "#DCEEEC" },
    { "type": "header", "content": "Espaciado" },
    { "type": "range", "id": "padding_top", "label": "Espacio superior (px)", "min": 0, "max": 160, "step": 8, "unit": "px", "default": 56 },
    { "type": "range", "id": "padding_bottom", "label": "Espacio inferior (px)", "min": 0, "max": 160, "step": 8, "unit": "px", "default": 40 }
  ],
  "presets": [ { "name": "Trotagua – PAS texto" } ]
}
{% endschema %}
